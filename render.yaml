services:
  - type: web
    name: s4-college-database-management
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: s4-college-db
          property: connectionString

databases:
  - name: s4-college-db
    plan: free
